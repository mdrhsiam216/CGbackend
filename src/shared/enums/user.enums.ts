export enum UserStatus {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
  SUSPENDED = 'suspended',
  PENDING = 'pending',
}

export enum UserRole {
  CLIENT = 'client',
  CAREGIVER = 'caregiver',
  ADMIN = 'admin',
}
